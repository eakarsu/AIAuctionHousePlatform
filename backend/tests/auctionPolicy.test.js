const test=require('node:test');const assert=require('node:assert/strict');const {moneyToCents,assertBid,bidHash,assertTransition}=require('../domain/auctionPolicy');
test('money conversion and ledger hash are stable',()=>{assert.equal(moneyToCents('12.34'),1234);assert.throws(()=>moneyToCents('1.234'),/decimals/);const a={lotId:1,bidderId:2,amountCents:1000,sequence:1,receivedAt:'2026-07-18T00:00:00Z'};assert.equal(bidHash(a),bidHash(a));});
test('bid and settlement gates reject invalid operations',()=>{assert.throws(()=>assertBid({state:'open',opensAt:'2026-07-18T00:00:00Z',closesAt:'2026-07-19T00:00:00Z',now:'2026-07-18T12:00:00Z',bidderVerified:false,amountCents:110,currentCents:100,minIncrementCents:10}),/Verified/);assert.throws(()=>assertTransition('closed','settled',{role:'finance',rationale:'reconciled payment and tax',closedBy:7,actorId:7}),/separation/);});
